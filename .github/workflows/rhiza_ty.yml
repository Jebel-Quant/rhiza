# This file is part of the jebel-quant/rhiza repository
# (https://github.com/jebel-quant/rhiza).
#
# Workflow: Ty
#
# Purpose: Run static type checking with ty to ensure
#          type safety across the codebase.
#
# Trigger: On push and pull requests to main/master branches.

name: "(RHIZA) TY"

permissions:
  contents: read

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  ty:
    name: Static type checking with ty
    runs-on: ubuntu-latest
    container:
      image: ghcr.io/astral-sh/uv:0.9.30-bookworm

    steps:
      - uses: actions/checkout@v6

      - name: Configure git auth for private packages
        uses: ./.github/actions/configure-git-auth
        with:
          token: ${{ secrets.GH_PAT }}

      # to brutal for now
      # - name: Run ty
      #   run: make -f .rhiza/rhiza.mk ty
